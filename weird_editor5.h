// Sample input 5, in CPP.

#include <cassert>

long long GetNumberLength() {
  return 1LL;
}

long long GetDigit(long long i) {
  switch ((int)i) {
    case 0: return 0LL;
    default: assert(0);
  }
}
